import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Register = () =>{
    return (<>
    <SafeAreaView className="items-center m-safe-offset-5">
        <Text className="" style={{fontSize : "1em"}} >Fidge!</Text>
    </SafeAreaView>
    </>);

}

export default Register